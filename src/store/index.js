import { createStore } from "vuex";
// import { showError } from '@/utils'
import api from '@/api';

export default createStore({
  strict: true,

  state: () => ({
  }),

  getters: {
  },

  mutations: {
  },

  actions: {
  }
});
